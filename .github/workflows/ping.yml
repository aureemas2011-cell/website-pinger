name: Scheduled Website Ping

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch: 

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Open Website
        run: curl -I -s https://travels.aureemas.com/
